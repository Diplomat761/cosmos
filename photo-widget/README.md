# 📸 Виджет просмотра фотографий из Яндекс.Диска

Виджет для просмотра случайных фотографий из публичной папки Яндекс.Диска без использования официального API.

## 📁 Структура проекта

```
photo-widget/
├── index.html    # Разметка виджета
├── style.css     # Стили и анимации
├── script.js     # Логика работы виджета
└── README.md     # Документация
```

## 🚀 Запуск

1. Откройте `index.html` в браузере
2. Виджет готов к использованию!

Для локального запуска можно использовать любой веб-сервер:

```bash
# Python 3
python -m http.server 8000

# Node.js (http-server)
npx http-server

# PHP
php -S localhost:8000
```

Затем откройте в браузере: `http://localhost:8000`

## ✨ Функциональность

- ✅ Просмотр фотографий с навигацией (Предыдущее/Следующее)
- ✅ Случайный выбор фотографии
- ✅ Автопрокрутка с настраиваемым интервалом (2-60 секунд)
- ✅ Индикатор загрузки с анимацией
- ✅ Счетчик текущего фото (номер / общее количество)
- ✅ Адаптивный дизайн (мобильные устройства)
- ✅ Современный интерфейс с градиентами и анимациями

## 📝 Подключение реальных фотографий

**Текущая конфигурация:** Виджет уже настроен на использование фотографий из папки `images/` проекта (файлы по маске `DSC*.JPG`). Всего загружено **233 фотографии**.

Если вы хотите использовать другой источник фотографий, следуйте инструкциям ниже.

### Вариант 1: Статический JSON файл (самый простой способ)

Этот метод идеально подходит для локальных проектов или когда у вас есть прямой доступ к файлам.

#### 📋 Пошаговая инструкция:

**Шаг 1: Создание файла photos.json**

Создайте файл `photos.json` в папке `photo-widget/` рядом с `index.html`.

**Шаг 2: Получение прямых ссылок на изображения**

Если вы используете фотографии из Яндекс.Диска:

1. Откройте публичную папку: https://disk.yandex.ru/d/SAFWsIFSGPhzOQ
2. Для каждого изображения:
   - Кликните на файл, чтобы открыть его
   - В браузере скопируйте прямую ссылку на изображение
   - Обычно ссылка выглядит так: `https://disk.yandex.ru/i/XXXXXXXXXXXXXX` или `https://getfile.dokpub.com/yandex/get/...`

**Альтернатива для локальных файлов:**

Если фотографии находятся в папке проекта (например, `images/`), используйте относительные пути:

```json
{
  "photos": [
    "../images/DSC07057.JPG",
    "../images/DSC07056.JPG",
    "../images/DSC07054.JPG"
  ]
}
```

**Шаг 3: Заполнение photos.json**

Откройте `photos.json` в текстовом редакторе и заполните его в следующем формате:

```json
{
  "photos": [
    "https://disk.yandex.ru/i/photo1-link",
    "https://disk.yandex.ru/i/photo2-link",
    "https://disk.yandex.ru/i/photo3-link",
    "https://disk.yandex.ru/i/photo4-link"
  ]
}
```

**Важно:** 
- Каждая ссылка должна быть в кавычках
- Ссылки разделяются запятыми
- Последняя ссылка не должна иметь запятую после себя
- Файл должен быть в формате UTF-8

**Шаг 4: Обновление script.js**

В файле `script.js` найдите функцию `init()` и замените её на:

```javascript
async function init() {
  try {
    // Пытаемся загрузить список фотографий из JSON файла
    const response = await fetch('photos.json');
    
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    
    const data = await response.json();
    
    // Проверяем, что данные в правильном формате
    if (data.photos && Array.isArray(data.photos) && data.photos.length > 0) {
      state.photos = data.photos;
      console.log(`Загружено ${data.photos.length} фотографий из photos.json`);
    } else {
      throw new Error('Неверный формат данных в photos.json');
    }
  } catch (error) {
    console.error('Ошибка загрузки списка фотографий:', error);
    console.log('Используется резервный список фотографий');
    // Используем временный массив как fallback
    state.photos = [...TEMP_PHOTOS];
  }
  
  // Устанавливаем обработчики событий
  setupEventListeners();
  
  // Загружаем первую фотографию
  loadPhoto(0);
  
  // Обновляем интерфейс
  updateUI();
}
```

**Шаг 5: Проверка работы**

1. Убедитесь, что файл `photos.json` находится в той же папке, что и `index.html`
2. Откройте `index.html` в браузере
3. Откройте консоль разработчика (F12) и проверьте, что нет ошибок
4. Должно появиться сообщение: `Загружено X фотографий из photos.json`

**Шаг 6: Решение проблем**

**Проблема:** "Failed to fetch" или "CORS error"
- **Решение:** Запустите локальный веб-сервер (см. раздел "Запуск" выше). Файлы нельзя открывать напрямую через `file://` из-за политики безопасности браузеров.

**Проблема:** "Неверный формат данных"
- **Решение:** Проверьте синтаксис JSON. Убедитесь, что:
  - Все кавычки закрыты
  - Нет лишних запятых
  - Файл начинается с `{` и заканчивается `}`
  - Массив `photos` содержит строки в кавычках

**Проблема:** Фотографии не загружаются
- **Решение:** 
  - Проверьте, что ссылки на изображения рабочие (откройте их в браузере)
  - Для локальных файлов убедитесь, что пути указаны правильно относительно `index.html`
  - Проверьте консоль браузера на наличие ошибок 404

**Пример готового photos.json для локальных файлов:**

```json
{
  "photos": [
    "../images/DSC07057.JPG",
    "../images/DSC07056.JPG",
    "../images/DSC07054.JPG",
    "../images/DSC07053.JPG",
    "../images/DSC07050.JPG"
  ]
}
```

### Вариант 2: Бэкенд-прокси

Создайте простой сервер (Node.js/Python/PHP), который будет хранить массив ссылок и отдавать их в формате JSON.

**Пример на Node.js (Express):**

```javascript
// server.js
const express = require('express');
const app = express();

app.get('/get-photos', (req, res) => {
  res.json({
    photos: [
      "https://disk.yandex.ru/d/SAFWsIFSGPhzOQ/photo1.jpg",
      "https://disk.yandex.ru/d/SAFWsIFSGPhzOQ/photo2.jpg"
    ]
  });
});

app.listen(3000, () => console.log('Server running on port 3000'));
```

В `script.js` замените загрузку на:
```javascript
async function loadPhotosFromServer() {
  try {
    const response = await fetch('http://ваш-сервер:3000/get-photos');
    const data = await response.json();
    state.photos = data.photos;
  } catch (error) {
    console.error('Ошибка:', error);
  }
}
```

## 🎨 Кастомизация

### Изменение цветовой схемы

В `style.css` найдите градиенты и измените цвета:

```css
/* Основной градиент фона */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* Градиенты кнопок */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

### Изменение размеров

```css
/* Размер области для фото */
.photo-container {
  height: 500px; /* Измените на нужное значение */
}
```

### Интервал автопрокрутки по умолчанию

В `script.js` измените:
```javascript
autoplayInterval: 10, // секунд
```

## 🔧 Технические детали

- **Языки**: HTML5, CSS3, Vanilla JavaScript (ES6+)
- **Браузеры**: Все современные браузеры (Chrome, Firefox, Safari, Edge)
- **Зависимости**: Нет внешних библиотек

## 📌 Текущий статус

- [x] Виджет использует фотографии из папки `images/` (DSC*.JPG)
- [x] Поддержка загрузки из `photos.json` (с fallback на локальные фото)
- [x] Обработка ошибок загрузки
- [ ] Добавить кэширование изображений
- [ ] Реализовать полноэкранный режим просмотра
- [ ] Добавить поддержку клавиатуры (стрелки влево/вправо, пробел)

## 📄 Лицензия

Свободное использование для личных и коммерческих проектов.

